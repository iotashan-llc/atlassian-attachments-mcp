{
  "name": "atlassian-attachments-mcp",
  "version": "2.0.0",
  "description": "Local MCP server for Jira & Confluence Cloud attachments — moves real file bytes without a shell, and writes Jira ADF without dropping embedded images",
  "license": "MIT",
  "author": "iotashan",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/iotashan-llc/atlassian-attachments-mcp.git"
  },
  "type": "module",
  "bin": {
    "atlassian-attachments-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "prepublishOnly": "npm run build && npm test"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "jira",
    "confluence",
    "atlassian",
    "attachments"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
